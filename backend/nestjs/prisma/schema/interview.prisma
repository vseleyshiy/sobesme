model Interview {
  id       String     @id @default(uuid())
  user     User       @relation(fields: [userId], references: [id])
  userId   String     @map("user_id")
  grade    Grade
  topic    String
  status   StatusType @default(IN_PROGRESS)
  score    Float?
  feedback String?

  messages Message[]

  createdAt DateTime @default(now()) @map("created_at")

  @@map("interviews")
}
